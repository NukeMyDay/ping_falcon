# Server port
PORT=3000

# Polling interval in seconds (default: 60)
POLL_INTERVAL=60

# Webhook signing key (used if subscriber doesn't provide their own secret)
WEBHOOK_DEFAULT_SECRET=change-me-to-a-random-string

# Rate limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100
